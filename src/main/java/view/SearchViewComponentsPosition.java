package view;

/**
 * Defines component positions for the SearchView to maintain a consistent layout.
 */
public class SearchViewComponentsPosition {

    // Title
    /** X coordinate of the title. */
    public static final int TITLE_X = 10;
    /** Y coordinate of the title. */
    public static final int TITLE_Y = 0;
    /** Width of the title. */
    public static final int TITLE_WIDTH = 200;
    /** Height of the title. */
    public static final int TITLE_HEIGHT = 20;

    // Distance Controls
    /** X coordinate of the distance label. */
    public static final int DISTANCE_LABEL_X = 10;
    /** Y coordinate of the distance label. */
    public static final int DISTANCE_LABEL_Y = 450;
    /** Width of the distance label. */
    public static final int DISTANCE_LABEL_WIDTH = 100;
    /** Height of the distance label. */
    public static final int DISTANCE_LABEL_HEIGHT = 20;

    /** X coordinate of the distance input field. */
    public static final int DISTANCE_INPUT_X = 120;
    /** Y coordinate of the distance input field. */
    public static final int DISTANCE_INPUT_Y = 450;
    /** Width of the distance input field. */
    public static final int DISTANCE_INPUT_WIDTH = 150;
    /** Height of the distance input field. */
    public static final int DISTANCE_INPUT_HEIGHT = 20;

    // Dish Type Controls
    /** X coordinate of the dish type label. */
    public static final int DISH_TYPE_LABEL_X = 10;
    /** Y coordinate of the dish type label. */
    public static final int DISH_TYPE_LABEL_Y = 490;
    /** Width of the dish type label. */
    public static final int DISH_TYPE_LABEL_WIDTH = 100;
    /** Height of the dish type label. */
    public static final int DISH_TYPE_LABEL_HEIGHT = 20;

    /** X coordinate of the dish type combo box. */
    public static final int DISH_TYPE_COMBO_BOX_X = 120;
    /** Y coordinate of the dish type combo box. */
    public static final int DISH_TYPE_COMBO_BOX_Y = 490;
    /** Width of the dish type combo box. */
    public static final int DISH_TYPE_COMBO_BOX_WIDTH = 150;
    /** Height of the dish type combo box. */
    public static final int DISH_TYPE_COMBO_BOX_HEIGHT = 20;

    // Search Button
    /** X coordinate of the search button. */
    public static final int SEARCH_BUTTON_X = 310;
    /** Y coordinate of the search button. */
    public static final int SEARCH_BUTTON_Y = 450;
    /** Width of the search button. */
    public static final int SEARCH_BUTTON_WIDTH = 100;
    /** Height of the search button. */
    public static final int SEARCH_BUTTON_HEIGHT = 60;

    // Map Area
    /** X coordinate of the map area. */
    public static final int MAP_AREA_X = 10;
    /** Y coordinate of the map area. */
    public static final int MAP_AREA_Y = 30;
    /** Width of the map area. */
    public static final int MAP_AREA_WIDTH = 400;
    /** Height of the map area. */
    public static final int MAP_AREA_HEIGHT = 400;

    // Zoom Controls
    /** X coordinate of the zoom label. */
    public static final int ZOOM_LABEL_X = 310;
    /** Y coordinate of the zoom label. */
    public static final int ZOOM_LABEL_Y = 520;
    /** Width of the zoom label. */
    public static final int ZOOM_LABEL_WIDTH = 100;
    /** Height of the zoom label. */
    public static final int ZOOM_LABEL_HEIGHT = 20;

    /** X coordinate of the zoom-in button. */
    public static final int ZOOM_IN_BUTTON_X = 310;
    /** Y coordinate of the zoom-in button. */
    public static final int ZOOM_IN_BUTTON_Y = 550;
    /** Width of the zoom-in button. */
    public static final int ZOOM_BUTTON_WIDTH = 48;
    /** Height of the zoom-in button. */
    public static final int ZOOM_BUTTON_HEIGHT = 30;

    /** X coordinate of the zoom-out button. */
    public static final int ZOOM_OUT_BUTTON_X = 362;
    /** Y coordinate of the zoom-out button. */
    public static final int ZOOM_OUT_BUTTON_Y = 550;
}
